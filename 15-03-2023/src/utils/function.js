export const textDescription = (str) =>
  str.split(" ").splice(0, 6).join(" ") + "...";

  export const titleDescription = (str) =>
  str.split(" ").splice(0, 2).join(" ") + "...";


  //tramite lo split  trasformo la stringa in oggetto []
  //con joing lo riporto a stringa 

  //procedimento fatto per la descrizione del prodotto e per il titolo del prodotto